name: CI Feed Service
on:
  push:
    branches: [ "main", "dev" ]
    paths:
      - 'services/feed-service/**' # Trigger on service changes
      - 'common-events/**'         # Trigger on shared events changes
      - 'common-security/**'       # Trigger on shared security changes

  pull_request:
    branches: [ "main", "dev" ]
    paths:
      - 'services/feed-service/**'
      - 'common-events/**'
      - 'common-security/**'

jobs:
  call-template:
    uses: ./.github/workflows/service-ci.yaml
    with:
      service_name: 'feed-service'
      service_path: 'services/feed-service'
    secrets: inherit # Pass repo secrets automatically